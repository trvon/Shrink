def heuristic:
	print("System Validity Check")

def __init__(self, validity):
	self.validity = []
	self.siteinfo = siteinfo
	self.organization = organization
	self.trust = trust
	self.contactability = contactability
	self.professional = professional

def computabilitycheck():
	for value in range validity:
		
		dataheuristic = 0
		validityheuristic = 0
		if self.siteinfo = self.value:
			dataheuristic += 1
		else if self.organization = self.value:
			dataheuristic += 1
		else if self.trust = self.value:
			validityheuristic += 1
		else if self.contactability = self.value:
			dataheuristic += 1
		else if self.professional = self.value:
			heuristic += 1
		else:
			value = 0







	
		 




